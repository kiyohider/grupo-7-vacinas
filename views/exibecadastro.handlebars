{{#each postos}}


<form action="/editarP" method="POST">
    <p>{{dataValues.Nome}}</p>
    <p>{{dataValues.Email}}</p>
    <p>{{dataValues.localizacao}}</p>
    <button type="submit">editar</button>

</form>




{{/each}}